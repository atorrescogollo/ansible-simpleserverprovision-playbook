127.0.0.1 {{ inventory_hostname.split('.')[1] }} {{ inventory_hostname }}

{% for host in groups['servers'] %}
{{ ansible_ssh_host }}  {{ hostvars[host]['inventory_hostname'].split('.')[1] }}	{{ hostvars[host]['inventory_hostname'] }}
{% endfor %}
